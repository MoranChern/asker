services:
  kg-neo4j:
    image: neo4j:2026.01.4-community
    ports:
      - '58274:7474'
      - '58287:7687'
    environment:
      - NEO4J_AUTH=neo4j/1kcsy2C7Vrn9JHuh
    volumes:
      - ./neo_data:/data
    # stdin_open: true # -i interactive
    # tty: true # -t tty